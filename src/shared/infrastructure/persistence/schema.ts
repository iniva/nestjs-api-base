// Central schema barrel — re-export all feature schemas here so
// drizzle-orm and drizzle-kit have a single entry point.
export * from '@/features/users/infrastructure/persistence/schema/user.schema'
